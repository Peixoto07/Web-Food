<nav class="fixed top-0 left-0 right-0 z-[60] bg-white shadow-sm px-2" x-data="{ open: false, activeLink: null }">
    <div class="container  mx-auto px-2 sm:px-6 lg:px-8">
        <div class="relative flex items-center justify-between h-16">
            <!-- Logo -->
            <div class="flex-shrink-0">
                <a href="#" class="text-xl font-bold text-gray-900">
                    <span class="text-black">B7</span><span class="text-yellow-500"> • </span><span class="text-gray-800">Delivery</span>
                </a>
            </div>

            <!-- Menu Items for larger screens -->
            <div class="hidden sm:block sm:ml-6">
                <div class="flex space-x-4">
                    <a href="#" :class="{ 'bg-orange-500 text-white': activeLink === 'Inicio' }" @click="activeLink = 'Inicio'"
                        class="text-gray-800 px-3 py-2 rounded-md text-sm font-medium">Início</a>
                    <a href="#" :class="{ 'bg-orange-500 text-white': activeLink === 'Burgers' }" @click="activeLink = 'Burgers'"
                        class="text-gray-800 px-3 py-2 rounded-md text-sm font-medium">Burgers</a>
                    <a href="#" :class="{ 'bg-orange-500 text-white': activeLink === 'Servicos' }" @click="activeLink = 'Servicos'"
                        class="text-gray-800 px-3 py-2 rounded-md text-sm font-medium">Serviços</a>
                    <a href="#" :class="{ 'bg-orange-500 text-white': activeLink === 'Contato' }" @click="activeLink = 'Contato'"
                        class="text-gray-800 px-3 py-2 rounded-md text-sm font-medium">Contato</a>
                </div>
            </div>

            <!-- Hamburger Menu Button -->
            <div class="sm:hidden">
                <button @click="open = !open" class="text-gray-800 focus:outline-none">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>

    <!-- Slide-in Menu -->
    <div x-show="open" x-transition:enter="transition ease-out duration-300"
        x-transition:enter-start="-translate-x-full" x-transition:enter-end="translate-x-0"
        x-transition:leave="transition ease-in duration-300" x-transition:leave-start="translate-x-0"
        x-transition:leave-end="-translate-x-full"
        class="fixed inset-0 bg-white z-[100] w-64 h-full shadow-lg flex flex-col p-4 space-y-4">
        
        <!-- Close Button -->
        <button @click="open = false" class="self-end text-gray-800 focus:outline-none">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
        </button>

        <!-- Login Button -->
        <a href="#" class="bg-orange-500 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-orange-600">
            Fazer Login
        </a>

        <!-- Menu Items -->
        <a href="#" :class="{ 'text-orange-500': activeLink === 'Cardapio' }" @click="activeLink = 'Cardapio'"
            class="text-gray-800 px-3 py-2 text-sm font-medium flex items-center hover:border-orange-500 hover:border-b-2">
            <span class="material-icons">Cardápio</span> 
        </a>
        <a href="#" :class="{ 'text-orange-500': activeLink === 'Sacola' }" @click="activeLink = 'Sacola'"
            class="text-gray-800 px-3 py-2  text-sm font-medium flex items-center hover:border-orange-500 hover:border-b-2">
            <span class="material-icons">Sacola</span> 
        </a>
        <a href="#" :class="{ 'text-orange-500': activeLink === 'Favoritos' }" @click="activeLink = 'Favoritos'"
            class="text-gray-800 px-3 py-2  text-sm font-medium flex items-center hover:border-orange-500 hover:border-b-2">
            <span class="material-icons">Favoritos</span> 
        </a>
        <a href="#" :class="{ 'text-orange-500': activeLink === 'MeusPedidos' }" @click="activeLink = 'MeusPedidos'"
            class="text-gray-800 px-3 py-2  text-sm font-medium flex items-center hover:border-orange-500 hover:border-b-2">
            <span class="material-icons">Pedidos</span> Meus 
        </a>
        <a href="#" :class="{ 'text-orange-500': activeLink === 'Configuracoes' }" @click="activeLink = 'Configuracoes'"
            class="text-gray-800 px-3 py-2  text-sm font-medium flex items-center hover:border-orange-500 hover:border-b-2">
            <span class="material-icons">Configurações</span> 
        </a>
    </div>
</nav>
